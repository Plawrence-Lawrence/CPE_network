the goal of the game is to compete to find the shortest path in a routing graph
between two nodes.

To run: run QuickRoute.py

Note: hosting acts as a server and not a player.
hosting simply manages the game.

By default it requires two clients with win condition of 3 rounds

